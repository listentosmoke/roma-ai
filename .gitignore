node_modules/
dist/
.vite/
.DS_Store
.env
.env.*
!.env.example
artifacts/
.bench-audio/
.testdata/
roma-tts.*
data/*.db
data/*.db-*
# machine-local agent/tooling state (contains machine-specific paths)
.claude/
.agents/
.codex/
# downloaded ML model cache (face encoder ~190 MB; re-fetched on demand)
.models/

# virtual-hardware lab outputs (generated media cache + run reports)
.simcache/
.simreports/

# stray scratch input, not part of the project
transcriptfromyoutube.txt

# Face fixtures are photographs of real people, fetched on demand
# (npm run fetch:face-fixtures), never committed.
test/fixtures/faces/

# Enrollment face images (redundancy for the templates) — never committed.
data/faces/
